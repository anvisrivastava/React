// import the React and ReactDom libraries
//Get the reference to the div with ID root
//Tell React to take control of that element
// Show the component on the screen

// React:-Library that defines what a component is and hoe multiple compnents work together.

//React:-Library that knows how to get a component to show up on the browser.

// import React from 'react';
// import ReactDom from 'react-dom/client';

// const el=document.getElementById('root');
// const root=ReactDom.createRoot(el);